{
  "name": "@meridian/backend",
  "version": "0.0.0",
  "private": true,
  "description": "API gateway, indexer, auth/KYC, and services. Placeholder.",
  "scripts": {
    "build": "echo \"no build yet\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0",
    "lint": "echo \"no lint yet\" && exit 0"
  }
}
